{
  "name": "demoblaze-tests",
  "version": "1.0.0",
  "description": "Playwright functional tests for DemoBlaze",
  "scripts": {
    "test": "npx playwright test --reporter=list,html",
    "test:headed": "npx playwright test --headed --reporter=list,html",
    "test:debug": "npx playwright test --debug",
    "report": "npx playwright show-report"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.38.0",
    "typescript": "^5.2.2"
  }
}